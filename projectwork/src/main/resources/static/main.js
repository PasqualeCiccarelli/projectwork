import { checkUtenteStatus } from 'controlloUtenteLoggato.js';
// import { loadCart, updateCartBadge, getCartCount } from 'cartUtils.js';

document.addEventListener('DOMContentLoaded', async () => {

    checkUtenteStatus();

    // const cartCount = await getCartCount();

    // updateCartBadge(cartCount);

    // loadCart();
});